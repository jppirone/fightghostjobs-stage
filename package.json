{
  "name": "fightghostjobs-stage",
  "private": true,
  "version": "0.1.0",
  "description": "FightGhostJobs staging site: static HTML/CSS/ES modules, no build step. `npm test` runs the checks in tests/.",
  "type": "module",
  "scripts": { "test": "node tests/run-all.js" }
}
